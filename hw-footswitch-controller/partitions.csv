# Name,     Type, SubType,   Offset,    Size,      Flags
# 16MB flash total (0x1000000 bytes). No OTA - single "factory" app slot.
# The data partition is named "spiffs" on purpose (not a typo) - that's
# the exact label ESP32's LittleFS.begin(true) looks for by default in
# PresetStore.cpp; the label doesn't need to match the filesystem type,
# it just needs to match what the code asks for.
nvs,       data, nvs,       0x9000,    0x5000,
app0,      app,  factory,   0x10000,   0x300000,
spiffs,    data, spiffs,    0x310000,  0xCE0000,
coredump,  data, coredump,  0xFF0000,  0x10000,
